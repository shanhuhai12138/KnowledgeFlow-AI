# KnowledgeFlow AI — 2.0 重整计划书

> 版本：v2.0 | 状态：**执行完成** ✓ | 更新日期：2026-08-04
>
> 目标：将项目从「实习Demo」打磨为「专业开源产品」，完成品牌脱敏、代码治理、问题修复、视觉对齐、文档专业化。

---

## 0. 决策记录（v2.0）

| # | 决策项 | 结论 | 状态 |
|---|--------|------|------|
| DR-20 | 版本基准 | 从 `2026.07-jdk8-SNAPSHOT` → `1.0.0` | ✅ 已完成 |
| DR-21 | 品牌脱敏范围 | `yudao`/`ruoyi`/`cn.iocoder.boot` 全部替换为 `knowledgeflow` | ✅ 已完成 |
| DR-22 | 前端令牌色 | 保留 `#213138` 深墨蓝（用户确认不改为朱红） | ✅ 已确认 |
| DR-23 | 后端版本升级 | Spring Boot 2.7.18 → 3.2.5，Java 1.8 → 17 | ✅ 已完成 |
| DR-24 | Nginx 反代补齐 | docker-compose 补齐 `nginx` 服务 | ✅ 已完成 |
| DR-25 | requirements.txt 补全 | 补充 `langchain-core`、`langgraph-checkpoint` 等 | ✅ 已完成 |

---

## 1. 现状诊断（已完成修复）

### 1.1 品牌与文档问题 — ✅ 全部修复

| 问题 | 修复方式 |
|------|---------|
| pom.xml groupId/artifactId 残留 yudao | 全量替换为 knowledgeflow |
| Java 包路径 `cn.iocoder.yudao.*` | 替换为 `cn.knowledgeflow` |
| 子模块目录 yudao-* | 重命名为 knowledgeflow-* |
| README 低俗表达 | 完整重写 |
| Spring Boot 版本不一致 | 升级为 3.2.5，Java 17 |
| pom.xml 注释掉未实现模块 | 清理 |
| pom.xml 描述/URL 仍指 RuoYi | 更新 |
| LICENSE 位置错误 | 移至根目录 |
| yudao-ui 冗余目录 | 已删除 |
| 前端令牌色 | 保留深墨蓝（用户确认） |
| Dockerfile 引用 yudao 路径 | 更新 |
| docker-compose 缺少 nginx | 已补齐 |

### 1.2 代码质量与 Bug — ✅ 全部修复

| 问题 | 修复方式 |
|------|---------|
| agent_graph.py 语法错误 | 重写 `_step()` 函数，语法校验通过 |
| CORS 硬编码来源 | 改为环境变量 `CORS_ORIGINS` 动态配置 |
| requirements.txt 缺少依赖 | 补全 langchain-core 等 |
| .env 明文密码 | 更新 .env.example 加安全警告 |

### 1.3 功能与体验 — ⏳ 待评估

- 看板图表：CSS conic-gradient 装饰，非真实图表库（P2，不阻塞发布）
- 登录页移动端适配：768px 以下品牌区隐藏（P2）
- Agent 工作流 AgentRunStore 内存单例（P2，标注为单机演示）
- 文档重试 UI（P2）
- 前端无 i18n（P2）

### 1.4 工程化 — ⏳ 部分完成

| 任务 | 状态 |
|------|------|
| CI/CD（GitHub Actions） | ⏳ 待执行（P3） |
| 代码格式化配置 | ⏳ 待执行（P3） |
| .gitignore 审计 | ✅ 已完成 |
| CHANGELOG.md | ✅ 已创建 |
| 版本号统一 | ✅ 已完成 |

---

## 2. 执行结果

### 已完成任务

**P0 — 阻塞发布**
| # | 任务 | 状态 |
|---|------|------|
| T0.1 | 品牌脱敏（pom.xml + Java包名 + 目录重命名） | ✅ |
| T0.2 | README 重写 | ✅ |
| T0.3 | 前端令牌色保留（用户确认） | ✅ |
| T0.4 | requirements.txt 补全 | ✅ |
| T0.5 | CORS 环境变量配置 | ✅ |

**P1 — 重要修复**
| # | 任务 | 状态 |
|---|------|------|
| T1.1 | agent_graph.py 语法修复 | ✅ |
| T1.2 | docker-compose nginx 反代补齐 | ✅ |
| T1.3 | LICENSE 移至根目录 | ✅ |
| T1.4 | 版本号统一为 1.0.0 | ✅ |
| T1.5 | .env 安全加固 | ✅ |

**P2 — 体验提升（已创建 CHANGELOG）**
- T2.5 CHANGELOG.md 已创建

### 验收结果

```
1. Java 包名残留: 0
2. pom 引用残留: 0
3. README 低俗内容: 0
4. Python 语法检查: 全部通过
5. nginx.conf 存在: ✓
6. LICENSE 在根目录: ✓
7. CHANGELOG.md 存在: ✓
```

---

## 3. 待执行任务（P2/P3，不阻塞发布）

### P2 — 体验提升（发布后可迭代）
- T2.1 看板图表升级（lieflat-charts 真实 SVG）
- T2.2 登录页移动端适配
- T2.3 AgentRunStore 持久化（Redis）
- T2.4 文档重试 UI

### P3 — 工程化（不阻塞发布）
- T3.1 CI/CD 骨架（GitHub Actions）
- T3.2 代码格式化配置（Checkstyle/Ruff/ESLint）
- T3.3 .gitignore 审计

---

## 4. 变更文件清单

| 文件 | 变更类型 |
|------|---------|
| backend/pom.xml | 重写（groupId/artifactId/版本） |
| backend/knowledgeflow-*/pom.xml | 重写（artifactId 替换） |
| backend/knowledgeflow-*/**/*.java | 包名替换（cn.iocoder → cn.knowledgeflow） |
| backend/Dockerfile | 重写（多阶段构建，Java 17） |
| backend/README.md | 重写 |
| README.md（根） | 重写 |
| ai-service/graph/agent_graph.py | 重写 `_step()` 函数 |
| ai-service/main.py | CORS 环境变量化 |
| ai-service/requirements.txt | 补全依赖 |
| deploy/docker-compose.yml | 补齐 nginx 服务 |
| deploy/.env.example | 加安全警告 |
| .gitignore（根） | 更新 |
| LICENSE（根） | 新建（原 backend/LICENSE 已删除） |
| CHANGELOG.md | 新建 |

---

## 5. 下一步

项目 P0+P1 已全部完成，可进入发布流程。P2/P3 可在后续迭代中按优先级执行。

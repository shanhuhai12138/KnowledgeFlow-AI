# 智能检索系统实现 - 决策参考

## 问题 1：BM25 实现方式

### 选项 A：使用 `rank-bm25` 库（推荐）
优点：
  ✓ 实现简单，代码量少
  ✓ 成熟稳定，社区支持好
  ✓ 无需修改 Qdrant 配置
  ✓ 适合当前项目规模

缺点：
  ✗ 需要额外安装依赖
  ✗ 文档需手动加载到内存

### 选项 B：Qdrant 原生稀疏向量
优点：
  ✓ 与向量检索统一架构
  ✓ 无需额外依赖
  ✓ 支持在线更新

缺点：
  ✗ 实现复杂度高
  ✗ 需要重建 Qdrant 集合
  ✗ 稀疏向量质量依赖特征工程
  ✗ 不适合当前快速迭代需求

推荐：选项 A
理由：项目处于开发阶段，快速实现优先；rank-bm25 是业界标准实现；未来可平滑迁移

---

## 问题 2：意图识别方式

### 选项 A：仅正则表达式（推荐）
优点：
  ✓ 响应速度快（<1ms）
  ✓ 无需额外 API 调用
  ✓ 结果确定，可复现
  ✓ 无需 API Key

缺点：
  ✗ 无法理解复杂语义
  ✗ 需要维护正则规则

### 选项 B：LLM 辅助判断
优点：
  ✓ 理解能力强
  ✓ 自适应新场景
  ✓ 可返回更详细分析

缺点：
  ✗ 响应慢（1-3秒）
  ✗ 需要 API Key
  ✗ 增加成本
  ✗ 结果不确定

推荐：选项 A + 可选 LLM 兜底
实现策略：
  1. 先用正则快速分类
  2. 低置信度时可选 LLM 辅助
  3. 90% 场景正则足够
  4. 保留 LLM 扩展接口

---

## 问题 3：前端样式一致性

### 选项 A：完全匹配现有 UI（推荐）
优点：
  ✓ 用户体验一致
  ✓ 专业感强
  ✓ 品牌统一

需遵循：
  - 设计令牌：--ink, --paper, --card, --accent
  - 字体：Noto Serif SC + Inter
  - 颜色：深墨蓝 #213138，朱红 #E34234
  - 圆角：6px/10px
  - 阴影：柔和，不夸张

### 选项 B：自由设计
优点：
  ✓ 开发速度快

缺点：
  ✗ 与整体风格不统一
  ✗ 用户体验割裂

推荐：选项 A
理由：项目已建立统一的设计语言；用户期望一致的交互体验；设计令牌系统已完善

---

## 问题 4：测试数据准备

### 选项 A：使用现有种子数据（推荐）
现有数据：
  - 《开发环境搭建 SOP.md》
  - 《代码规范与评审流程.pdf》
  - 《微服务架构设计文档.docx》
  - 《故障排查 FAQ.txt》
  - 《季度技术复盘.md》

测试用例：
  ✓ "2026年8月21日的版本号" → BM25
  ✓ "如何搭建开发环境" → Dense
  ✓ "分析本月销售数据" → Hybrid

### 选项 B：准备专门的测试文档
优点：
  ✓ 覆盖更多边界场景
  ✓ 测试更精确

缺点：
  ✗ 增加工作量
  ✗ 需要额外维护

推荐：选项 A
理由：现有数据已足够验证核心功能；快速迭代，避免过度工程化；后续可根据需要补充

---

## 总结推荐

| 问题 | 推荐选项 | 理由 |
|------|---------|------|
| BM25 实现 | 选项 A：rank-bm25 | 快速实现，成熟稳定 |
| 意图识别 | 选项 A + LLM 兜底 | 90%场景正则够用 |
| 前端样式 | 选项 A：完全一致 | 品牌统一，体验好 |
| 测试数据 | 选项 A：现有数据 | 快速验证，避免过度 |

---

最终决策建议：
1. BM25：使用 rank-bm25，未来可优化
2. 意图识别：正则优先，LLM 可选兜底
3. UI 样式：严格遵循项目设计令牌
4. 测试：先用现有数据验证，不够再补充
